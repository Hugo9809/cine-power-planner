{
  "name": "cine-power-planner",
  "version": "1.0.1",
  "description": "Browser-based tool for planning professional camera setups powered by V-Mount or B-Mount batteries. It calculates total power consumption, current draw at 14.4 V and 12 V, and estimated battery runtime while checking that the battery can safely deliver the required power.",
  "main": "data.js",
  "scripts": {
    "lint": "eslint .",
    "check-consistency": "node checkConsistency.js",
    "normalize": "node normalizeData.js",
    "unify-ports": "node unifyPorts.js",
    "generate-schema": "node generateSchema.js",
    "test": "npm run lint && npm run check-consistency && npm run test:jest",
    "test:jest": "npm run test:unit && npm run test:ui",
    "test:unit": "npm run test:data && npm run test:storage",
    "test:script": "node --max-old-space-size=4096 node_modules/jest/bin/jest.js --runInBand tests/ui/script.integration.test.js",
    "test:data": "jest --runInBand tests/data",
    "test:storage": "jest --runInBand tests/storage",
    "test:ui": "npm run test:script && npm run test:ui:components",
    "test:ui:components": "jest --runInBand tests/ui/sharedLinkButton.test.js tests/ui/utils.test.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@eslint/js": "^9.31.0",
    "eslint": "^9.31.0",
    "globals": "^16.3.0",
    "jest": "^30.0.4",
    "jest-environment-jsdom": "^30.0.4",
    "jest-localstorage-mock": "^2.4.26"
  },
  "dependencies": {
    "lz-string": "^1.5.0"
  }
}
