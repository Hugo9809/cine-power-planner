const mock = {
  compressToEncodedURIComponent: s => s,
  decompressFromEncodedURIComponent: s => s,
  compressToUTF16: s => s,
  decompressFromUTF16: s => s,
  compress: s => s,
  decompress: s => s
};

export default mock;
