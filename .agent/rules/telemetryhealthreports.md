---
trigger: always_on
---

Every new feature must include basic, anonymous telemetry to track success rates and performance. If a feature is failing for users, the development team should know via automated alerts before the user has to report it.