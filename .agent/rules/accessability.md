---
trigger: always_on
---

All UI elements must be navigable via keyboard and compatible with screen readers (using appropriate ARIA labels and semantic HTML). High contrast and scalable text support are mandatory for all new views.